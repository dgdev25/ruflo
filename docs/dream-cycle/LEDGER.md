# Dream Cycle Ledger

Durable memory across nightly Dream Cycle sessions on `dgdev25/ruflo`. Created 2026-08-14
(did not exist before — the very first commit in this fork's git history to actually use
the `dream(...)` commit-message convention pairs with PR #1 on 2026-08-13, which never
appended a ledger row; that gap is backfilled below from PR #1's own body/evidence trail
rather than left blank, since a durable ledger with a missing first row would misinform
every future cycle's STEP 1 check).

| Date | Deep | Finding | Issue | PR | Evaluated? | Verdict | Effect | Witness | Prior-night fates |
|---|---|---|---|---|---|---|---|---|---|
| 2026-08-13 | memory | `smartSearch()` multi-query fan-out was sequential; `Promise.all` concurrency fix | LOCAL (Issues disabled, 410) | #1 (open, draft) | yes | ACCEPT | 2.9x-5x latency reduction, byte-identical output | `498a9cea...` | n/a (first tracked night) |
| 2026-08-14 | swarm | `hooksPreTask`'s computed `complexity` bucket was dead for agent-count purposes; wired it into `suggestAgentsForTask`'s recommendation with a security-role safety exemption | LOCAL (Issues disabled, 410) | #2 (open, draft) | yes | ACCEPT | Low-bucket mean agent count 2.90→1.48 (n=21), medium/high unchanged, 0 regressions, 0 safety violations | `c3ed8320...` | 2026-08-13 PR #1: OPEN, draft, <24h old, pending human review — not stale (>14d threshold), no action needed yet |
| 2026-08-15 | performance | `FlashAttention.cpuOptimizedAttention()`'s `expBuffer` sized off `useTopK` alone but branch taken is gated by `useTopK && numK>128`; for 32<numK<=128 this silently produced NaN output (undersized buffer, reads past `.length` return `undefined`). Fixed sizing predicate to match branch predicate; added `rmse` quality metric that caught the bug | LOCAL (Issues disabled, 410, reconfirmed) | #3 (MERGED 2026-08-15) | yes | ACCEPT | NaN→finite (RMSE~1e-9) for 33-128 keys; byte-identical outside that range; independently re-confirmed with a 2nd, code-sharing-free repro | `b271917b...` | PR #1 (2026-08-13): OPEN, draft, 2d old, not stale. PR #2 (2026-08-14): OPEN, draft, 1d old, not stale. 0/14 (2 total) prior candidate PRs merged — biased tonight toward a small single-file candidate |
| 2026-08-16 | security | `SafeExecutor.validateCommand()`/`isCommandAllowed()` authorized any command string whose `path.basename()` matched an allowlisted bare name — `/tmp/attacker/git` passed the same check as `git`. Same bug class as OpenClaw CVE-2026-32973 (CVSS 9.8) and Cursor CVE-2026-22708. Fixed: path-like commands now require an exact allowlist match; bare-name basename leniency preserved. **Backfilled 2026-08-17** — this row was never appended the night PR #4 was opened (a real ledger gap, not a fabricated one; found via STEP 1's GitHub PR audit, content reconstructed from PR #4's own body/evidence trail, same precedent as this ledger's original PR #1 backfill note) | LOCAL (Issues disabled, 410, reconfirmed) | #4 (open, draft) | yes | ACCEPT | Decision-boundary ASR 100%→0% (5/5 blocked); 0% regression on legitimate usage; real end-to-end proof-of-exploit (spawned sentinel binary executes on baseline, blocked on candidate) | `3fd654ce...` | PR #3 (2026-08-15): MERGED same day (~8h). PRs #1/#2: still OPEN/draft, no reviewer comments yet. 1/3 (33%) prior candidate PRs merged as of that night |
| 2026-08-17 | intelligence | `EWCConsolidator.updateFisherFromConfidences()` blended `globalFisher = alpha*old+(1-alpha)*new` (`alpha=fisherDecayRate`, default 0.01) while the 2 production-wired sibling methods (`computeFisherMatrix()`/`recordGradient()`, same config field, same state) use `(1-decay)*old+decay*new` — inverted EMA weighting meant this path discarded ~99% of accumulated Fisher history per call instead of retaining ~99%, defeating EWC's purpose the moment it activates. Zero production callers today (dead code, confirmed via grep) — fixed before it activates. Independent adversarial critic: CONFIRMED, 1 evidence-accuracy correction made (see PR) | LOCAL (Issues disabled, 410, reconfirmed) | #5 (open, draft) | yes | ACCEPT | 20-step Fisher-history retention: baseline 9.11e-10 → candidate 0.8179 (analytical (1-alpha)^20=0.8179, matches to 7 sig figs); 4 unrelated methods byte-identical; Darwin: shipped variant fitness 1.00 vs rejected variant 0.35 | `ecf6c62a...` | PR #3 (2026-08-15): MERGED. PR #4 (2026-08-16): OPEN, draft, 1d old, not stale — backfilled tonight, see row above. PRs #1/#2: OPEN, draft, 4d/3d old, not stale yet (14d threshold), 0 reviewer comments on any of the 4 open/closed PRs to date. 1/4 (25%) prior candidate PRs merged |
